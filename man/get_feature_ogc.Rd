% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_feature_ogc.R
\name{get_feature_ogc}
\alias{get_feature_ogc}
\title{Download features from an OGC API Features service}
\usage{
get_feature_ogc(
  url,
  collection,
  bbox = NULL,
  datetime = NULL,
  properties = NULL,
  cql_filter = NULL,
  limit = NULL,
  crs = NULL,
  quiet = TRUE,
  ...
)
}
\arguments{
\item{url}{A character string with the base URL of the OGC API (the landing
page).}

\item{collection}{A character string with the ID of the collection (layer).}

\item{bbox}{A bounding box to filter features. Can be a numeric vector of
length 4 (\code{c(xmin, ymin, xmax, ymax)}) or an object of class \code{bbox} (from
\code{sf::st_bbox()}). Note: OGC APIs generally expect the bbox coordinates to
be in WGS84 (EPSG:4326).}

\item{datetime}{A character string representing a time instant or time
interval (e.g., \code{"2018-02-12T23:20:50Z"} or
\code{"2018-02-12T00:00:00Z/2018-03-18T12:31:12Z"}).}

\item{properties}{A character vector of specific column names to return.}

\item{cql_filter}{A character string containing a CQL2-text filter to apply
attribute or complex spatial filtering on the server.}

\item{limit}{Numeric. Maximum number of features to retrieve. If \code{NULL}
(default), all available features will be fetched via automatic API
pagination.}

\item{crs}{Target Coordinate Reference System (CRS) as an EPSG code or CRS
object. If provided, the output will be automatically transformed to this
CRS.}

\item{quiet}{Logical. Should the download progress be suppressed? Defaults to
\code{TRUE}.}

\item{...}{Additional arguments passed on to \code{sf::read_sf()}.}
}
\value{
An \code{sf} (simple feature) object.
}
\description{
\code{get_feature_ogc()} provides a modern alternative to \code{get_feature_wfs()}. It
retrieves vector data from an OGC API Features service.
}
\examples{
\dontrun{
# Base URL for the Digitaal Vlaanderen Wegenregister (Road Register)
api_url <- "https://geo.api.vlaanderen.be/Wegenregister/ogc/features/v1"

# 1. Basic usage: Fetch 10 road segments
roads_sample <- get_feature_ogc(
  url = api_url,
  collection = "Wegsegment",
  limit = 10
)

# 2. Spatial Filtering & CRS Transformation:
# Fetch features within a WGS84 bounding box and project to Belgian Lambert 72
my_bbox <- sf::st_bbox(
  c(xmin = 4.39, ymin = 51.21, xmax = 4.40, ymax = 51.22),
  crs = sf::st_crs(4326)
)
roads_bbox <- get_feature_ogc(
  url = api_url,
  collection = "Wegsegment",
  bbox = my_bbox,
  crs = 31370
)

# Base URL for Historical Land Use (Ferraris map)
hist_url <- "https://geo.api.vlaanderen.be/HistLandgebruik/ogc/features/v1"

# 3. Attribute Selection:
# Fetch only specific columns
ferraris_lite <- get_feature_ogc(
  url = hist_url,
  collection = "Lgbrk1778",
  limit = 100,
  properties = c("KLASSE")
)

# 4. Advanced CQL2 Attribute Filtering:
# Filter features directly on the server
roads_filtered <- get_feature_ogc(
  url = api_url,
  collection = "Wegsegment",
  limit = 50,
  cql_filter = "morfologischeWegklasse = 'dienstweg'"
)
}
}
